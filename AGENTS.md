# Expo SDK 58

Read the exact versioned docs at https://docs.expo.dev/versions/v58.0.0/ before writing any code. This branch tracks the SDK 58 beta (`expo@next`).

Product language: `CONTEXT.md`. Decisions: `docs/adr/`. Conventions: `docs/agents/`.
